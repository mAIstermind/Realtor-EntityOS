# Antigravity Autonomous Team Manifest

## Core Architecture Role: System Architect Agent
* **Objective**: Ensure the Express/Node backend (`server.ts`) securely routes and serves data without memory leaks or environment exposure.
* **Execution Rule**: Public profile endpoints must remain read-only. Secure all raw database IDs using lower-case, sanitized URL slugs.

## Core Growth Role: GEO/AEO Engineering Agent
* **Objective**: Optimize public-facing HTML assets for maximum Synthesis Potential and information density.
* **Execution Rule**: Inject deep-nested JSON-LD `RealEstateAgent`, `AreaServed`, and `FAQPage` nodes on every public render. Format local knowledge strings into Answer-First `H2` prompt structures.
